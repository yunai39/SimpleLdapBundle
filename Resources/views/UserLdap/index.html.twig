{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserLdap list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idldap</th>
                <th>Valid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('userldap_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idLdap }}</td>
                <td>{{ entity.valid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('userldap_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('userldap_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('userldap_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
