{% extends '::admin.html.twig' %}

{% block body -%}
    <h1>{% trans %}admin.role.titleList{% endtrans %}</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>{% trans %}admin.role.rolename{% endtrans %}</th>
                <th>{% trans %}admin.role.actions{% endtrans %}</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('roleldap_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.roleName }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('roleldap_show', { 'id': entity.id }) }}">{% trans %}admin.show{% endtrans %}</a>
                    </li>
                    <li>
                        <a href="{{ path('roleldap_edit', { 'id': entity.id }) }}">{% trans %}admin.edit{% endtrans %}</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('roleldap_new') }}">
                {% trans %}admin.role.newEntry{% endtrans %}
            </a>
        </li>
    </ul>
    {% endblock %}
